# Schema Migration

Schema migrations apply controlled database changes and prevent breaking downstream analytics or inference jobs.

Operational note: Schema Migration supports reliable ML engineering workflows by making model behavior easier to test, deploy, monitor, and explain.
